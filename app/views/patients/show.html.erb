<h1><%= @patient.name %></h1>
<ul>
  <% @patient.appointments.each do |appointment| %>
    <li><%= link_to appointment.doctor_name, doctor_path(appointment.doctor) %></li>
    <li><%= appointment.appointment_datetime.strftime('%B %d, %Y at %H:%M') %></li>
  <% end %>
</ul>
