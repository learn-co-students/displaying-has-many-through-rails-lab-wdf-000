<h1><%= @patient.name %></h1>
<ul>
  <% @patient.appointments.each do |appointment| %>
  <li>
    Time: <%= appointment.appointment_datetime.strftime('%B %d, %Y at %R') %>
    <br>
    Doctor: <%= link_to appointment.doctor.name, appointment.doctor %>
  </li>
  <% end %>
</ul>