<h1>Welcome to Doctor Directory</h1>

<p>Please select a doctor:</p>

<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.name, doctor_path(doctor) %>
      <ul>
        <li><%= "Total patients: " + doctor.patients.count.to_s %></li>
      </ul>
    </li>
  <% end %>
</ul>