<h2><%= @doctor.name %>'s page</h2>
<ul>
<% @doctor.appointments.each do |app| %>
<li>
<%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> - <%= link_to app.patient.name, patient_path(app.patient) %> 
</li><br>
<% end %>
</ul>