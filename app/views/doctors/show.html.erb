
<%= @doctor.appointments.each do |appo| %>
  <%= appo.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
<% end %>
<br>
<%= @doctor.patients.each do |patient| %>
  <%= link_to patient.name, patient_path(patient) %>
<% end %>
