<h1><%= @doctor.name %></h1>
<h2>Department: <%= @doctor.department %></h2>

<h3>Appointments</h3>
<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> - <%= link_to appt.patient.name, appt.patient %></li>
  <% end %>
</ul>
