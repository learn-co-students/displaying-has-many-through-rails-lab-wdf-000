<h1><%= @doctor.name %></h1>
<h2><%= @doctor.department %></h2>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= link_to appointment.patient_name, patient_path(appointment.patient) %></li>
    <li><%= appointment.appointment_datetime.strftime('%B %d, %Y at %H:%M') %></li>
  <% end %>
</ul>
